# adapter-hono

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build adapter-hono` to build the library.

## Running unit tests

Run `nx test adapter-hono` to execute the unit tests via [Vitest](https://vitest.dev/).
